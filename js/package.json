{
  "name": "losungen",
  "version": "1.0.0",
  "description": "Heutiger Losungstext als HTML Nodes.",
  "main": "src/losungen.js",
  "scripts": {
    "start": "node src/losungen.js",
    "build": "esbuild src/losungen.js --bundle --outfile=build/bundle.js --target=es6",
    "test": "jest tests/losungen.test.js"
  },
  "jest": {
    "setupFilesAfterEnv": [
      "./jest.setup.js"
    ]
  },
  "author": "Max Nötzold <max.noetzold@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.25.4",
    "jest": "^29.7.0"
  },
  "dependencies": {
    "papaparse": "^5.5.2"
  }
}
